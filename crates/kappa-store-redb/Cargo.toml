[package]
name = "kappa-store-redb"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true
description = "Optional redb query accelerator for kappa-registry."

[lib]
name = "kappa_store_redb"
path = "src/lib.rs"

[features]
default = ["encryption"]
encryption = ["kappa-core/encryption"]

[dependencies]
# Not inherited: the workspace entry keeps default features on for every other
# member, and a member cannot turn off defaults the workspace entry leaves on.
kappa-core = { path = "../kappa-core", default-features = false }
redb = { workspace = true }
blake3 = { workspace = true }
hex = { workspace = true }
tracing = { workspace = true }
tempfile = { workspace = true }
uuid = { workspace = true }
sha2 = { workspace = true }
md-5 = "0.10"
serde = { workspace = true }
serde_json = { workspace = true }
getrandom = { workspace = true }
base64-simd = "0.8"
crc-fast = "1"
zstd = "0.13"
xz2 = "0.1"
bzip2 = "0.5"

[dev-dependencies]
